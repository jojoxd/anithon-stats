include:
  - project: tools/reviewdog
    ref: main
    file: /templates/eslint.yml

stages:
  - review

reviewdog:eslint:
  stage: review

  before_script:
    - apk add --update --no-cache python3 make g++ libc6-compat fontconfig
    - yarn install

  variables:
    COMMAND: 'yarn ci:lint'
