include:
  - project: tools/reviewdog
    ref: main
    file: /templates/eslint.yml

stages:
  - review

reviewdog:eslint:
  stage: review

  before_script:
    - apk add --no-cache --virtual .gyp python make g++ libc6-compat
    - yarn install

  variables:
    COMMAND: 'yarn ci:lint'
