import 'package:flutter/material.dart';

class SearchMediaPage extends StatelessWidget {
  const SearchMediaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Text('Search Media Page'),
    );
  }
}
