{
  "name": "@jojoxd/tsed-auth",
  "version": "0.0.1",
  "description": "Add Authentication to tsed",
	"dependencies": {
		"jexl": "^2.3.0"
	},
	"peerDependencies": {
    "@tsed/common": "^6.114.8",
    "@tsed/core": "^6.114.8",
    "@tsed/di": "^6.114.8",
		"@tsed/schema": "^6.114.8",
		"@tsed/exceptions": "^6.114.8",
    "@tsed/platform-params": "^6.114.8",
    "reflect-metadata": "^0.1.13"
  },
  "devDependencies": {
    "@jojoxd/tsconfig": "1.1.2",
		"@types/jexl": "^2.3.0",
    "@swc/core": "^1.2.179",
    "tslint": "^6.1.1",
    "tsup": "^5.12.6",
    "typescript": "^4.3.5"
  },
  "scripts": {
    "build:package-tsed-auth": "yarn clean && yarn tsc",
    "clean:package-tsed-auth": "yarn clean",
    "watch:package-tsed-auth": "yarn build:package-tsed-auth --watch",
    "clean": "rm dist -rf && echo 'Cleaned package-tsed-auth'",
    "tsc": "tsup index.ts --out-dir dist --format cjs,esm --dts "
  },
  "main": "dist/index.js"
}
